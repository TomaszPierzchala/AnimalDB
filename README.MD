# AnimalDB

AnimalDB is a laboratory animal management system for tracking mice, breeding, genealogy, and experimental data.

## Getting Started

Before running the application for the first time, create an empty PostgreSQL database named **animaldb** ([see database setup](dev.database/README.md)).

Then start the application:

```bash
./mvnw spring-boot:run
```